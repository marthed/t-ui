
// const isWithinRange = (range) => (match) => {
//   if (range.max && range.max > match )
// }

module.exports = async function filterMatches(matches, filters) {

  const filteredmatches = matches.filter((match) => {
    return true;
  });

  return filteredMatches;
}

